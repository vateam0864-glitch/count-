name: Local Deployment

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted   # <— your laptop
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run deploy script
        run: bash deploy.sh
